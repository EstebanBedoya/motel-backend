export enum RoomType {
  NORMAL = 'normal',
  SPECIAL = 'special',
}

export enum RoomState {
  AVAILABLE = 'available',
  OCCUPIED = 'occupied',
  CLEANING = 'cleaning',
  MAINTENANCE = 'maintenance',
}
